package com.recruit.recruitms.enumeration;

public class Enum {

    public enum Role {
        ADMIN,
        STAFF,
        USER
    }

    public enum Gender {
        MALE,
        FEMALE
    }

    public enum ObjectState {
        CREATED,
        TERMINATED,
        ACTIVE,
        PENDING,
        DRAFT,
        FROZEN
    }
}
