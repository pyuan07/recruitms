package com.recruit.recruitms.enumeration;

public class Enum {

    public enum Level {
        LOW,
        MEDIUM,
        HIGH
    }

    public enum ObjectState {
        TERMINATED,
        ACTIVE,
        PENDING,
        DRAFT,
        FROZEN
    }
}
